/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images:{
        domains:[
            'cdn-icons-png.flaticon.com'
        ]
    }

};

export default nextConfig;
